default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: <%= ENV['DB_HOST'] %>
  port: <%= ENV['DB_PORT'] %>
  username: <%= ENV['DB_USERNAME'] %>
  password: <%= ENV['DB_PASSWORD'] %>

development:
  primary:
    <<: *default
    database: commenter_development

test:
  primary:
    <<: *default
    database: commenter_test

production: &primary_production
  primary:
    <<: *default
    database: commenter_development
  queue:
    <<: *default
    database: commenter_production_queue
    migrations_paths: db/queue_migrate
